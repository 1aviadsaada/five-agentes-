# Claude Code local settings (per-machine, not part of project)
.claude/settings.local.json

# Environment files with secrets (the template .env.example stays tracked)
.env
.env.*
!.env.example

# Word/Office temporary lock files (created when opening .md in Word)
~$*
